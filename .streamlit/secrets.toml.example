# Streamlit Secrets Configuration
# WICHTIG: Diese Datei ist in .gitignore und sollte NIEMALS committed werden!

# AWS DynamoDB Credentials
# Ersetze die Werte mit deinen eigenen AWS Credentials
# Siehe DYNAMODB_SETUP.md für Details

[aws]
aws_access_key_id = "DEIN_AWS_ACCESS_KEY_ID"
aws_secret_access_key = "DEIN_AWS_SECRET_ACCESS_KEY"
aws_region = "eu-central-1"
dynamodb_table_name = "doppelkopf_sessions"
